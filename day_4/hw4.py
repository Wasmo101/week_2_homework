#Create a function which given an array of integers, return an array, where the first element is the count of
#positives numbers and the second element is sum of negative numbers. 0 is neither positive nor negative.
#If the input array is empty or null, return an empty array.
arr = [2, -4, 5, 3, 12, -104, -56, 27, 0]
negative_nums = []
def positive():
    if arr >= 0:
        arr.append(positive)
        return
    elif arr <= -1:
        arr.append(negative_nums)
        return
print(positive)
print(negative_nums)

        
    
#Create a function that returns the sum of the two lowest positive numbers given
#an array of minimum 4 positive integers. No floats or non-positive integers will be passed.
pos_arr = [6, 20, 48, 96, 109, 10034]
new_pos = []
for x in pos_arr:
    pos_arr.append(x(new_pos)[0],[1])
print(new_pos)



##Write a function that when given a list of items will return the 
##item that appears the most times in the list. 
#If two or more items appear the same amount of times, output all items in a list.

#Example Input = ['Orange', 'Apple', 'Bear', 3, 7, 'Tree', 'Orange', 'Tree']
#Example Out = ['Orange', 'Tree']

#Hint (a counter dictionary might be helpful)
order = ['Orange', 'Apple', 'Bear', 3, 7, 'Tree', 'Orange', 'Tree']
def newarr():
    for x in order():
        if x == 2:
            order.append(x, (order))
